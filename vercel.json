{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d ../api ] || [ -f ../server.js ]; then npm install; else npm install && npm install --prefix frontend; fi",
  "buildCommand": "if [ -d ../api ] || [ -f ../server.js ]; then npm run build; else npm run build --prefix frontend; fi",
  "outputDirectory": "frontend/dist",
  "functions": { "api/index.js": { "maxDuration": 60 } },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/health", "destination": "/api/index" },
    { "source": "/((?!api/|health).*)", "destination": "/index.html" }
  ]
}
