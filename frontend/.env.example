# Frontend (Vercel / local)
# StackLens frontend calls `/api/analyze` (same-origin).
# - In dev, Vite proxies `/api/*` to http://localhost:4000 (see vite.config.ts)
# - In prod, Vercel rewrites `/api/*` to your Render backend (see vercel.json)

